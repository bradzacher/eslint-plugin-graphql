import rules from './rules'
import processors from './processors'

// TODO - the old code did this... do we need this?
export { default as processors } from './processors'

export default {
  rules,
  processors
}
